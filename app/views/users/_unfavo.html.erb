<%= form_for(current_user.favoing_relationfavotweets.find_by(tweet_id: @micropost.id),
    html: { method: :delete },
    remote: true) do |f| %>
    <%= button_tag( class: "btnlink btnlink-warning") do %>
        <%= content_tag :span, "あんふぁぼ", class: "glyphicon glyphicon-star-empty" %>
    <% end %>
<% end %>
