<%= form_for(current_user.following_relationships.find_by(followed_id: @user.id),
            html: { method: :delete },
            remote: true) do |f| %>
    <%#= f.submit "アンフォローする", class: "btn" %>
    <%= button_tag( class: "btnlink btnlink-warning") do %>
      <%= content_tag :span, "アンフォローする", class: "glyphicon glyphicon-remove" %>
    <% end %>  
<% end %>
